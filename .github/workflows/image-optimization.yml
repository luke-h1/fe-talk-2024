name: Compress images
on:
  workflow_dispatch:
jobs:
  build:
    name: calibreapp/image-actions
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@master

      - name: Compress Images
        uses: calibreapp/image-actions@main
        with:
          githubToken: ${{ secrets.IMAGE_OPTIMIZATION_TOKEN }}
          jpegQuality: "70"
          jpegProgressive: true
          pngQuality: "70"
